#ifndef NDEBUG
#include "mkldnn.hpp"

namespace mkldnn {
//at::at(const primitive &aprimitive, size_t at = 0) {
//    : data(c_api::mkldnn_primitive_at(aprimitive.get(), at))
//        using namespace std;
//    if(at != 0){ cout<<" primitive.at("<<primitive<<"\n\t, at="<<at<<")"<<endl; }
//}
}//mkldnn::
#endif
